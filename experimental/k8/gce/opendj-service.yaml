apiVersion: v1beta3
kind: Service
metadata:
  name: opendj
  label:
    - name: opendj
spec:
  ports:
    - port: 389
      targetPort: ldap
      protocol: "TCP"
      name: opendj-389
  createExternalLoadBalancer: true
  selector:
    name: opendj
